package com.leap; // use your own package

public class LeapYear {

	
	public boolean calculateLeapYear(int year) {
		if(year%400==0 ||(year%4==0 && year%100!=0)){
			return true;
		}
		else {
			return false;
		}
	}
	}

